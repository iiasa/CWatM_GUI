# CWatM GUI - build-only dependencies (PyInstaller executable build)
#
# Install the runtime dependencies first, then the build tools:
#   pip install -r requirements.txt -r requirements_build.txt
#
# Note: GDAL is deliberately NOT listed - rasterio wheels ship their own GDAL
# (see cwtmexe.md); installing the GDAL wheel separately is unnecessary and has
# caused broken environments.

-r requirements.txt

# PyInstaller (pulls in altgraph, pefile, pywin32-ctypes, pyinstaller-hooks-contrib)
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6

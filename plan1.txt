I need to add a new button to my GUI application. Please **ultrathink** and create a comprehensive plan for:
- add a button "Check Data" right of the checkbox label "Write output..".
- the style of the button is the same as the other
- if you press the button it creates a new window
- the new window should have a similar style like the rest
- the new windows has 2 parts: 
- left part with some text , checkboxes and field
- left part: text on top "Check Data\n With this window you can check  the data of the settingsfile\n It will run CWatM but only analyse the data in the settingsfile\n\n .
- left part: below text: checkbox "Write output to:" and a field to put in the output file. Default output file is check_cwatm1.csv
- left Part below checkbox:  text: It can also be used to check against an existing output\n output must be a discharge... netcdf file\n"
- left part below text: a selection of a file box. It can only select "dischar*.nc" files
- left part: below text: field "Write output to:" put in the output file. Default output file is check_cwatm1.csv
- left part: below checkbox; Button "Start Check" to start CWatM with the flag -c and the file given in the file selection box
- Right window: a big text window which can display a .csv table
- after running "Start Check"it should load the file saved as "Write output to:" and display it as a table with ",'as separator
- skip the first 17 lines, but put in lines 7 to 16 at the bottom


if you press Run check in check Data window it will execute cwatm with the line:
success, checkinfo = run_cwatm.main(settingsfile, ['-c']) with the settingsfile given in load_txt. 
- put the stout text into the original output window
- in checkinfo is the csv table
- save checkinfo into file name given in "write Output to"
- display checkinfo as table in the right window as tbale from line 17 onward
- the check data window should be 50% bigger
 

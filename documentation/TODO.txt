TODO
=====


- add button to write messages and error to a defined file
- add menues -> settings
       - to write errors to a file
	   - define working dir


Added 2026-07-01
----------------
- Put an information text next to "Run CWatM" that shows when the MaskMap or the
  Gauges have been changed (i.e. a hint that the run will use the new values).
- [DONE 2026-07-01] Put a "Gauges" label and a text field under the MaskMap field,
  linked to the settings "Gauges" entry. On load/save the field text is coloured
  blue if the gauges are inside the mask map, red if outside (basin_viewer.py:
  gauges_in_maskmap).
- [DONE 2026-07-01] When Start Date, Spin Date, End Date, PathOut or MaskMap is
  changed, update the settings-file content in memory (but do NOT save it to disk).
- Think about a versioning system: when a Save is done, store the difference to the
  previous version together with the date (i.e. keep a dated diff history of saves).
- [DONE 2026-07-01] "Set Gauge" tool (Tools menu): sets Gauges to the cell centre with
  the largest upstream area (ups.nc) inside the mask map
  (basin_viewer.find_largest_ups_gauge).
- Add a "Create PathOut Folder" tool under the Tools menu (referenced by the "PathOut
  does not exists!" warning) to create the resolved PathOut directory.
